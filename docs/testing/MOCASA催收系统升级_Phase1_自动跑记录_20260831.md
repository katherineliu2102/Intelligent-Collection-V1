# Phase 1 自动跑记录（2026-08-31）

> **层级**：Pilot 自然日自动跑。昨晚 **17:39** 已空名单、消费跟订阅。  
> **对比 8/30**：8/30 还按 200 名单滤；今天两份 env 为空，日切全表扫。  
> **环境**：8/30 17:39 那包。StartedAt `2026-08-30T09:38:36Z`，loopback health **200**。  
> **取数**：2026-08-31 **09:44 PHT**（08:00 / 09:15 已过；12:00 Push、14:00 Email、14:30 AI 未到）。  
> **三天综述**：[8/29–8/31](./MOCASA催收系统升级_Phase1_自动跑综述_20260829-0831.md)  
> **关联**：[8/30 记录](./MOCASA催收系统升级_Phase1_自动跑记录_20260830.md) · [200 案抽样](./MOCASA催收系统升级_Phase1_e2e200抽样说明_20260829.md)

## 目录

- [1. 结论](#1-结论)
- [2. 进了多少](#2-进了多少)
- [3. 已过槽](#3-已过槽)
- [4. 数仓与日切](#4-数仓与日切)
- [5. 催收库表](#5-催收库表)
- [6. 问题清单](#6-问题清单)
- [7. 下午还要看](#7-下午还要看)

---

## 1. 结论

**消费跟订阅已经对齐**。今日 inbox **173** 条 `caseEvent`（03:00:03–22）：200 圈 **139** + 原 40 圈 **34**。原 40 进投影但计划昨晚已停，今日实发 **0**。S0 整桶仍不在订阅里，缺的还是那 **57**。

日切 `fullScan=true scanned=179`（投影里非 CEASED 的行，不再是 200 名单长度），`stageChanged=0`。08:00 SMS **127** DELIVERED；09:15 AI **126 通一批** `mocasa-20260831-0915-1`，接通 **0**。悬挂 **0**。今日还款取消（`505901`）已走 `skipOpenSteps`，没有新的 EXECUTING。

**新问题：8 个案 S1 计划昨天下午走完，今天投影已是 S2，但没有新计划，整日无触达。** 当时日切只在「有活跃计划且档变了」时发 `STAGE_CHANGED`。续档逻辑已改（见综述 §5.3），**尚未发到 Pilot**。


| 口径                    | 结果                                                                         |
| --------------------- | -------------------------------------------------------------------------- |
| 名单                    | **空**。订阅有什么进什么                                                             |
| inbox `caseEvent`     | **173**（200 圈 139 + 原 40 圈 34），03:00:03–22                                 |
| 200 圈投影               | 仍 **143**；今日快照 139，少的 4 个是昨已还/结清                                           |
| 缺 57                  | 与 8/30 同一批，S0 整桶仍未进                                                        |
| 日切 03:35              | `scanned=179` `fullScan=true` `stageChanged=0` `ceased=0`                  |
| 08:00 SMS             | tick `scanned=128`；**127** DELIVERED（全在 200 圈）                             |
| 09:15 AI              | tick `scanned=127`；**126 通一批**；BUSY 56 / FAILED 43 / NO_ANSWER 27；接通 **0** |
| 12:00 / 14:00 / 14:30 | 未到。Email 预写 **4** 步，投影 dpd=4 / S2                                          |
| 原 40 实发               | **0**                                                                      |
| 悬挂                    | **0**                                                                      |


---



## 2. 进了多少


| 项                         | 值                                                 |
| ------------------------- | ------------------------------------------------- |
| env 两份名单                  | **0**                                             |
| inbox 当日 `caseEvent`      | **173** 案                                         |
| 其中 200 圈                  | **139**                                           |
| 其中原 40 圈                  | **34**（例如 `466438` `482686` `489935` `520049`）    |
| 200 圈仍在 `t_ai_collection` | **143**（和 8/30 相同）                                |
| 200 圈今日无 `caseEvent`      | **4**：`520142` `530618` `531592` `531713`（昨已还/结清） |
| 200 圈仍缺投影                 | **57**，ID 与 8/30 相同                               |
| 200 圈活跃计划                 | **126**（S1 11 / S2 32 / S3 32 / S4 51）            |
| 原 40 活跃计划                 | **0**                                             |




### 2.1 原 40 进 inbox 但不打

空名单后，订阅里的原 40 不再被 ack 跳过。34 条 `caseEvent` 进了投影。最新计划是 `PLAN_CANCELLED`（32）或 `PLAN_COMPLETED`（2），昨晚 `MANUAL_CLEANUP` 仍生效，今日步骤 **0** 次实发。属预期。

### 2.2 S0 仍整桶不在

缺 57 没变：S0 约 40 + D91_already 4 + 逾期桶里像结清的十几个。今日 `WHITELIST_SKIPPED` 仍为 0，不是催收又滤掉的。请数仓看到期前提醒是否进每日快照。

### 2.3 已进 200 圈的档（今天投影）


| 投影                       | n                             |
| ------------------------ | ----------------------------- |
| S1 dpd 3                 | 11                            |
| S2 dpd 4 / 6 / 9         | 12 / 17 / 11                  |
| S3 dpd 18 / 25           | 20 / 12                       |
| S4 dpd 34 / 64 / 77 / 82 | 19 / 12 / 16 / 4              |
| IN_COLLECTION、stage 空    | 3（`520142` `531592` `530618`） |
| CEASED dpd 91            | 4                             |
| SETTLED                  | 2                             |


---



## 3. 已过槽


| PHT      | 渠道    | 今日         | 说明                                                                      |
| -------- | ----- | ---------- | ----------------------------------------------------------------------- |
| 03:35 日切 | —     | 全表 179     | 见 §4                                                                    |
| 08:00    | SMS   | **127**    | 全在 200 圈。另有 31 条原 40 残留步被昨晚 SKIPPED，扫描未捞                                |
| 09:15    | AI    | **126 一批** | Facade `84769096-…` / `mocasa-20260831-0915-1`；接通 0                     |
| 12:00    | PUSH  | 未到         | 预写 PENDING 132                                                          |
| 14:00    | EMAIL | 未到         | 预写 PENDING **4**（`506516` `531187` `531221` `531489`），投影 **dpd=4 / S2** |
| 14:30    | AI    | 未到         | 预写 PENDING 100                                                          |


SETNX 仍通过：09:15 一批。

6 条 08:00/09:15 仍显示 PENDING，全在 **已取消** 计划上（含 `520142` `531592` `531713` 和 3 个原圈）。超时/到期扫描不捞终态计划，不会发出。是残留行，不是新悬挂。

### 3.1 8 个案今天没计划（挡触达）

投影已是 S2 dpd=4，**没有活跃计划**，今日 SMS/AI/Email 都没有：

`506565` `519633` `531157` `531245` `531278` `531315` `531368` `531446`

上一条都是 8/30 的 S1 计划，**14:31 左右** `PLAN_COMPLETED`（S1 末日 14:30 走完）。今天 03:00 有 `caseEvent`、档已是 S2，但当时日切只在「有活跃计划」时比档。续档已改代码（最近一份 `PLAN_COMPLETED` 且档变则建），**尚未发到 Pilot**。

同一批 dpd=4 的另外 4 个（上面 Email 那 4 个）8/30 起就是 S2 计划，槽还在。

---



## 4. 数仓与日切



### 4.1 caseEvent — 通过（进了的那些）


| 层                 | 8/30                     | 8/31                                       |
| ----------------- | ------------------------ | ------------------------------------------ |
| inbox `caseEvent` | 03:00:04–27，143（仅 200 圈） | **03:00:03–22，173**（200 圈 139 + 原 40 圈 34） |
| 日切                | `scanned=200`（名单）        | `scanned=179` **全表**（184 投影 − 5 CEASED）    |


无死锁 nack，无当日 ERROR。

### 4.2 日切全表 — 通过（扫到了）

```
daily roll completed fullScan=true scanned=179 stageChanged=0 ceased=0
```

空名单 + `daily-roll-full-scan-enabled` 默认 true 已生效。后续 tick 是 `full scan already completed today; skip`。

`stageChanged=0` 对「计划档已经等于投影档」的案正常；对 §3.1 那 8 个是当时漏续建（代码已补，待发版）。

### 4.3 repaymentEvent（只记账，不验 2h）

4 案 18 条（00:00–09:45）：`505901` `530618` `531687` `531713`。

- `505901`：计划 `1017` `REPAID`，打开步 **39 条 SKIPPED**，EXECUTING=0。新包收口有效。
- `531687`：仍 `STEP_SCHEDULED`，12:00 / 14:30 各 1 条 PENDING。更像未整笔结清，不是悬挂。
- `531713` / `530618`：昨已取消；取消计划上还留着预写 PENDING，扫描不捞。

---



## 5. 催收库表


| 表                          | 8/31 09:44                            |
| -------------------------- | ------------------------------------- |
| `t_ai_collection`          | 184（143 新圈 + 原 Pilot 残留）              |
| `t_ai_collection_inbox` 当日 | 173 `caseEvent` + 18 `repaymentEvent` |
| 悬挂 `EXECUTING`             | **0**                                 |
| `t_event_outbox` PENDING   | 0                                     |
| `t_event_dlq` 当日           | 0                                     |


timeline 当日 OUT：SMS 127、AI_CALL 126。

---



## 6. 问题清单


| 项                    | 挡触达？             | 状态                       |
| -------------------- | ---------------- | ------------------------ |
| S0 整桶仍无 `caseEvent`  | 这 40 今天仍无触达      | 与 8/30 同一批 57；请数仓看到期前提醒  |
| 8 个案 S1 走完未续 S2      | 上午空窗；11:58 已补 S2 | **已发版 + 手动日切**：8 案均 `S2 PENDING`，保留 12:00/14:00/14:30 |
| 原 40 进 inbox 不打      | 否                | 昨晚停计划，属预期                |
| 取消计划上残留 PENDING      | 否                | 扫描不捞；新取消已 skipOpen       |
| Email 4 步是否按当天 dpd 发 | 待 14:00          | 预写案投影已是 dpd=4，不要用桶名对     |


昨晚那两条 14:30 悬挂、话术档不刷新：取数时 EXECUTING=0；stage 刷新已在 17:39 包里，今日 SMS/Push 正文未抽。

---



## 7. 下午还要看

- 12:00 Push 是否约 126–132（`531687` 若仍在催会多 1）
- 14:00 Email 4 步：正文/模板应对 **dpd=4**，不是桶名
- 14:30 AI 是否一批；还款案不得再留 EXECUTING
- 那 8 个 S2：11:53 发版、清 Redis `daily-roll:2026-08-31:completed` 后 11:58 手动 `dailyRoll`。`scanned=179 stageChanged=10 ceased=0 rollbackSkipped=0`。8 案均新建 S2（计划 1035–1044），步骤 12:00 Push / 14:00 Email / 14:30 AI 仍 PENDING。顺带救了 `513749` `526109` S2→S3。

