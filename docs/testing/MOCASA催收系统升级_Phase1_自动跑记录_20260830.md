# Phase 1 自动跑记录（2026-08-30）

> **层级**：Pilot 自然日自动跑。昨晚切 **200 案**白名单后的首跑。  
> **对比 8/29**：8/29 白天仍是原 40 案；**23:12** 两份名单换成 e2e200，未重编 jar。  
> **环境**：取数时镜像仍是 8/27 **16:32**。**17:39 PHT** 已发新包并清空名单，见 §7。  
> **取数**：2026-08-30 **16:46 PHT**；接通对话与 57 案补记于 **17:10**；发版核对 **17:39**。  
> **三天综述**：[8/29–8/31](./MOCASA催收系统升级_Phase1_自动跑综述_20260829-0831.md)  
> **关联**：[8/29 记录](./MOCASA催收系统升级_Phase1_自动跑记录_20260829.md) · [200 案抽样](./MOCASA催收系统升级_Phase1_e2e200抽样说明_20260829.md)

## 目录

- [1. 结论](#1-结论)
- [2. 200 案进了多少](#2-200-案进了多少)
- [3. 五槽实绩](#3-五槽实绩)
- [4. 数仓：03:00 进库](#4-数仓0300-进库)
- [5. 催收库表](#5-催收库表)
- [6. 问题清单](#6-问题清单)
- [7. 明天](#7-明天)

---

## 1. 结论

**200 案分流开跑了，但只进了 143。** 白名单两份都是 200、与 CSV 一致。今日 inbox 的 143 条 `caseEvent` 全是这 200 里的新 `caseId`，03:00:04–27 进库，早于日切。原 40 案今日 **0** 次实发（扫描名单已换，残留 PENDING 没捞）。

进投影的 143 案：08:00 SMS / 12:00 Push 各 143 DELIVERED；09:15 AI **143 通一批**；14:30 AI **104 通一批** + 2 悬挂。接通 **1** 通（`513849`）：约 77 秒、核名一句、无借款人发言；当日第二通 CONNECT_AND_STOP 已跳过。Email 无槽（周日，精确日已错开）。

57 个白名单 ID 今日无 `caseEvent`（`WHITELIST_SKIPPED=0`）。逾期桶缺的 13 个**很像抽样后结清**；S0 整桶 40 **不像两天内全还清**；D91_already 4 个本就是停催圈。日切按名单 `scanned=200`，缺投影 WARN，「无案件」，`stageChanged=0`。数仓侧已说明 Topic 只发这 200（或过滤订阅）。

| 口径 | 结果 |
|---|---|
| 200 案分流 | **进 143 / 200**。投影 143、计划 143、今日步骤只打这 143。缺 57：S0 整桶 40 + D91_already 4 + 其余 13 |
| 日切 03:35–05:55 | `scanned=200`（名单长度）；`stageChanged=0` `ceased=0` `rollbackSkipped=0`；缺案 WARN |
| 08:00 SMS | tick `scanned=143`；143 DELIVERED |
| 09:15 AI | **143 通一批** `mocasa-20260830-0915-1`；BUSY 62 / FAILED 45 / NO_ANSWER 35 / ANSWERED **1** |
| 12:00 PUSH | tick `scanned=143`；143 DELIVERED |
| 14:00 EMAIL | tick `scanned=0`（无预写槽） |
| 14:30 AI | tick `scanned=106`；**104 通一批** `mocasa-20260830-1430-1`；BUSY 44 / FAILED 38 / NO_ANSWER 22；**EXECUTING 2** |
| `caseEvent` 进库 | **03:00:04–27**，143 条，早于日切 |
| `repaymentEvent` | 5 案共 51 条（12:30–16:45）。回看窗是大约估计，**不验收是否刚好 2h** |

---

## 2. 200 案进了多少

取数时 env / 容器内两份名单仍是 200，与 [e2e200 CSV](./e2e200_loan_ids_20260829.csv) 一致，样例 `469165,468179,468279`。

| 项 | 值 |
|---|---|
| 白名单 | SCAN / PILOT 各 **200**，互为全集 |
| inbox 当日 `caseEvent` | **143**，caseId 全在 200 内，无名单外 |
| 200 案在 `t_ai_collection` | **143** |
| 200 案有计划 | **143** |
| 200 案今日 `executed_at` | SMS 143、PUSH 143、AI 09:15 的 143 + 14:30 的 106 |
| 原 40 案今日实发 | **0** |

### 2.1 未进投影的 57（是不是已还款）

数仓已说明：`intelligent-collection-cases-v1` **只发这 200**（或另有过滤订阅）。接入今日 `WHITELIST_SKIPPED=0`，这 57 个不是我们又滤掉的，是**今天这条订阅里没有他们的 `caseEvent`**。

本机 BQ 连不上还款表，不能逐案钉 `clear_time`。按桶看：

| 桶 | 缺 | 更像什么 |
|---|---|---|
| S1 / S2 / S4 共 13 | `520194,531529,531556,531718,531727,531771,506532,506697,519627,519673,530569,504801,520076` | **抽样后结清**。和 8/28 那几个已还款未捞同一类；逾期桶里只缺零星几个，不像整段被上游拿掉 |
| D91_already 4 | `469165,468179,468279,468491` | **停催圈**。8/28 抽样已是 DPD≈140，契约上可不再发在催快照，不必当成「刚还清」 |
| S0 四档 40 | 整桶 | **不太像两天内 40 户全还清**。抽样 8/29 应为 DPD≈−3，今天约 −2，仍在 `dpd >= -3`。更像：今日快照没把到期前提醒发出来，或 8/29 已发过被旧 40 名单 ack 掉、今天没再发 |

日切仍按 200 名单滚，缺行只 WARN，不挡已进的 143。

### 2.2 已进 143 的档

以今天投影为准（不要只用桶名）：

| 投影 | n |
|---|---|
| S1 dpd 2 / 3 | 11 / 12 |
| S2 dpd 5 / 8 | 18 / 11 |
| S3 dpd 17 / 24 | 20 / 12 |
| S4 dpd 33 / 63 / 76 / 81 / 90 | 19 / 12 / 16 / 4 / 4 |
| IN_COLLECTION、stage 空、dpd −29/−26 | 3（`520142` `531592` `530618`，当日有还款） |
| SETTLED | 1（`531713`） |

---

## 3. 五槽实绩

| PHT | 渠道 | 今日 | 说明 |
|---|---|---|---|
| 03:35 日切 | — | 名单 200 | 无升档/停催；缺投影的 ID 只 WARN |
| 08:00 | SMS | **143** | tick `08:00:04 scanned=143` |
| 09:15 | AI | **143 一批** | Facade `0cd8caa4-…` / `mocasa-20260830-0915-1` |
| 12:00 | PUSH | **143** | tick `12:00:04 scanned=143` |
| 14:00 | EMAIL | **0** | 无步骤。D+1/D+4/D+75 是 8/29 的精确日，今天已是 +2/+5/+76 |
| 14:30 | AI | **104 一批** + 2 悬挂 | Facade `18f05875-…` / `mocasa-20260830-1430-1`；tick `scanned=106` |

原 40 案 09:15 槽上仍有 **36** 条 PENDING（35 个旧 caseId，`520049` 两条），扫描名单不含他们，没发出。属预期。

SETNX 仍通过：09:15 / 14:30 各一批。

### 3.1 接通（案 `513849`）

| 项 | 值 |
|---|---|
| 身份 | plan `996` / step `10708` / 审计 id=303 / `session_id=97da9742-…` |
| 判定 | `line_outcome.was_ai_connected=true` 且 `reason=NORMAL` → `ANSWERED`；验签通过 |
| 时间 | 09:15:10 起呼，09:16:27 回写，**约 77 秒**（`dialed_at` 09:15:59 UTC+8） |
| 停呼 | 同计划 14:30 step `10725` 在 09:16:28 **SKIPPED**；未进 14:30 那批 |
| 计划 | 仍 `STEP_SCHEDULED` / S3，8/31 起还有预写槽（CONNECT_AND_STOP 只停当日第二通） |

对话：催收库无正文，用 Facade key 拉 `script_url`（595 字节）。**只有助手一句核名，没有借款人发言，没有承诺，`summary` 空。**

1. Magandang araw po, si Maria po ito mula sa Mocasa. Si PATRICK HERNANDEZ ARBILO po ba ang kausap ko?

和 8/28 的 `513749` 同类：线路算接通，对客几乎没谈成。那次是四句助手、约 11 分钟；这次更短，核名后就挂了。录音同域，同样要 Authorization。

### 3.2 14:30 悬挂

| case | step | plan | 现象 |
|---|---|---|---|
| `531592` | 4628 | 898 | 14:30:04 `EXECUTING`，`timeout_time` **NULL**；计划已 `PLAN_CANCELLED` |
| `520142` | 4659 | 904 | 14:30:05 `EXECUTING`，同样 NULL；计划已 `PLAN_CANCELLED` |

两案当天有 `repaymentEvent`（12:30 / 13:15 起），投影 dpd 已到 −29。取消计划后步骤停在 EXECUTING，超时列为空，`callbackTimeout` 整天 `scanned=0`，不会收敛。取数时已挂约 2 小时。

---

## 4. 数仓：03:00 进库

### 4.1 caseEvent — 通过（进了的那些）

| 层 | 8/29 | 8/30 |
|---|---|---|
| inbox `caseEvent` | 03:00:02–27，34 条（旧 40 圈） | **03:00:04–27**，**143** 条（200 圈） |
| 日切 | 已是当天快照 | 同上；`rollbackSkipped=0` |

03:00 有一批 `DeadlockLoserDataAccessException` → nack 重投，最终 143 都进了，无名单内漏单。

### 4.2 repaymentEvent（只记账，不验 2h）

回看窗是大约估计，**不是验收项**。今日 5 案 51 条，15 分钟节奏还在；计划仍只取消一次。`531713` 已 SETTLED；`531592` / `520142` 取消后踩中 §3.2 悬挂。

---

## 5. 催收库表

| 表 | 8/30 16:46 |
|---|---|
| `t_ai_collection` | 184（143 新圈 + 原 Pilot 残留） |
| `t_ai_collection_inbox` 当日 | 143 `caseEvent` + 51 `repaymentEvent` |
| 悬挂 `EXECUTING` | **2**（§3.2） |
| `t_event_outbox` PENDING | 0 |
| `t_event_dlq` 当日 | 0 |

timeline 当日 OUT：SMS 143、PUSH 143、AI_CALL 247（09:15 的 143 + 14:30 已回写的 104）。

---

## 6. 问题清单

| 项 | 挡触达？ | 状态 |
|---|---|---|
| 57 案今日无 `caseEvent` | 这 57 今天无触达 | 13 个逾期桶像结清；S0 整桶不像全还清；D91_already 是停催圈 |
| 14:30 两步悬挂 | 这两步未终态 | 还款取消计划后 `timeout_time` 空，超时扫描捞不到 |
| 话术档跟旧 snapshot.stage | 对客措辞 | 催收代码仍未改；今日未抽 SMS/Push 正文 |
| 原 40 残留 PENDING | 否 | 扫描不捞 |
| 03:00 死锁 nack | 否 | 重投后齐 |

---

## 7. 今晚已切：消费跟订阅（17:39 PHT）

已发新包并清空两份名单。以后加量/换名单走数仓 Publisher，不必再改 `COLLECTION_PILOT_LOAN_IDS` / `COLLECTION_SCAN_CASE_IDS`。

| 项 | 值 |
|---|---|
| 镜像 | 在 8/27 `4ca65f5` 底上叠：空名单可启、日切全表默认开、取消计划收口 EXECUTING、发送前刷新当天 stage |
| StartedAt | `2026-08-30T09:38:59Z`（17:38:59 PHT），loopback health **200** |
| 两份名单 | **空**。启动日志：`loan-id-whitelist 为空：消费订阅内全部案件`；`ScanIsolationGuard` 空名单告警放行 |
| 日切 | `daily-roll-full-scan-enabled` 默认 **true**（env 未写则走 YAML） |
| 原 40 | 发版前已 `MANUAL_CLEANUP` 取消活跃计划，PENDING/EXECUTING 标 SKIPPED |
| 8/30 两步悬挂 | 发版前 SQL 已收口；新包还款取消会走 `skipOpenSteps` |
| 回滚 | jar `/opt/app/build/collection-admin.jar.bak.202608301732`；env `/opt/app/pilot.env.bak.20260830093728`（仍是 200 名单） |

8/31 实绩见 [8/31 记录](./MOCASA催收系统升级_Phase1_自动跑记录_20260831.md)。全日：日切全表 179；8 案下午三槽已补上；悬挂 0；S0 仍未进；原 40 有 2 案被续档打到。
