# Phase 1 自动跑记录（2026-08-29）

> **层级**：Pilot 自然日自动跑。计划切 **200 案分流**；实际仍是原 **40** 案白名单。  
> **对比 8/28**：数仓改 03:00 进库、还款回看 10h→2h；200 案抽样见 [说明](./MOCASA催收系统升级_Phase1_e2e200抽样说明_20260829.md)。  
> **环境**：镜像仍是 8/27 **16:32** 那版（未重编 jar）。**23:12 PHT** 只改两份白名单并重启，StartedAt `2026-08-29T15:12:59Z`。  
> **取数**：白天实绩 **22:10 PHT**；名单切换 **23:13 PHT**。  
> **三天综述**：[8/29–8/31](./MOCASA催收系统升级_Phase1_自动跑综述_20260829-0831.md)  
> **关联**：[8/28 记录](./MOCASA催收系统升级_Phase1_自动跑记录_20260828.md) · [200 案抽样](./MOCASA催收系统升级_Phase1_e2e200抽样说明_20260829.md)

## 目录

- [1. 结论](#1-结论)
- [2. 200 案没有进入测试](#2-200-案没有进入测试)
- [3. 原 40 案五槽实绩](#3-原-40-案五槽实绩)
- [4. 数仓复验：03:00 进库 / 还款 2h 窗](#4-数仓复验0300-进库--还款-2h-窗)
- [5. 催收库表](#5-催收库表)
- [6. 问题清单](#6-问题清单)
- [7. 明天](#7-明天)

---

## 1. 结论

**新抽的 200 案没有进入测试。** 白名单没换成这 200 个 `loan_id`，投影 / 计划 / 今日步骤全是 0。今天跑的仍是 8/27 起那 40 案。

原 40 案五槽按时、无悬挂、无 ERROR、PENDING outbox=0、当日 DLQ=0。09:15 / 14:30 仍各一批。Email 今日无槽（预告 0）。

数仓两项 **可记过**：`caseEvent` **03:00:02–27** 进催收库（早于 03:35 日切）；今日 inbox **没有** 再刷已结清案的 `repaymentEvent`。

| 口径 | 结果 |
|---|---|
| 200 案分流 | **未进**。扫描/接入白名单仍 40，与 200 **交集 0**；`t_ai_collection` 0 行；计划 0；今日步骤 0 |
| 日切 03:35–05:55 | `scanned=40`（不是 200）；`stageChanged=1`（`520049` S3→S4）；`rollbackSkipped=0` |
| 08:00 SMS | tick `scanned=32`；32 DELIVERED |
| 09:15 AI | **32 通一批** `mocasa-20260829-0915-1`；BUSY 20 / FAILED 9 / NO_ANSWER 3；接通 **0** |
| 12:00 PUSH | tick `scanned=32`；32 DELIVERED |
| 14:00 EMAIL | tick `scanned=0`（无预写槽） |
| 14:30 AI | **24 通一批** `mocasa-20260829-1430-1`；BUSY 14 / FAILED 6 / NO_ANSWER 4 |
| `caseEvent` 进库 | **03:00:02–27**，34 条，早于日切 |
| `repaymentEvent` | 当日 inbox **0** 条（8/28 已结清案不再连发） |

---

## 2. 200 案没有进入测试

Pilot env 取数 22:10：

| 项 | 值 |
|---|---|
| `COLLECTION_SCAN_CASE_IDS` | **40**，样例 `466438,482686,489935,…`（原 Pilot） |
| `COLLECTION_PILOT_LOAN_IDS` | **40**，与扫描名单相同 |
| e2e200 CSV | 200 个 `loan_id`，样例 `469165,468179,…` |
| 两份名单交集 | **0** |
| 200 案在 `t_ai_collection` | **0** |
| 200 案有 `t_contact_plan` | **0** |
| 200 案今日 `executed_at` | **0** |
| 日切 `scanned` | 整天都是 **40** |

接入白名单不包含这 200，数仓即使发了他们的 `caseEvent` 也不会进催收投影；扫描白名单不含他们，也不会捞步骤。容器未重启、env 未改，所以今天不可能跑到 200 案。

要进测：两份名单都换成 [e2e200_loan_ids_20260829.csv](./e2e200_loan_ids_20260829.csv)，重启 `collection-admin`，再看日切 `scanned` 和 inbox 是否出现这 200 的 `caseId`。

---

## 3. 原 40 案五槽实绩

对照 8/28 预估（40 案若仍在名单里）：

| PHT | 渠道 | 8/28 预估 | 今日 | 说明 |
|---|---|---|---|---|
| 03:35 日切 | — | 40 | **40** | `520049` dpd=32，S3→S4，`STAGE_CHANGED` |
| 08:00 | SMS | 33 | **32** | tick `08:00:07 scanned=32`；3 PENDING=已还款 |
| 09:15 | AI | 34 | **32 一批** | Facade `4353010a-…`；4 PENDING=取消计划 |
| 12:00 | PUSH | 34 | **32** | tick `12:00:04 scanned=32` |
| 14:00 | EMAIL | 0 | **0** | 无步骤 |
| 14:30 | AI | 26 | **24 一批** | Facade `0216049c-…` |

未捞原因：`502131` / `502356` / `527448` 仍 `PLAN_CANCELLED/REPAID`；`520049` 日切升档后旧 S3 计划 `STAGE_UPGRADE`，余下 09:15/12:00/14:30 留 PENDING。不是漏催。槽身份上仍有 8/26 提前打掉的旧 `original_trigger`，不计入今日实发。

09:15：`09:15:26 wave=20260829-0915#1 cases=32`。  
14:30：`14:30:26 wave=20260829-1430#1 cases=24`。SETNX 仍通过。

---

## 4. 数仓复验：03:00 进库 / 还款 2h 窗

### 4.1 caseEvent 进库 — 通过

| 层 | 8/28 | 8/29 |
|---|---|---|
| 催收 inbox `caseEvent` | 08:00:03–05 | **03:00:02–27**（34 条） |
| 日切首扫 | 读到的是昨日投影 → `rollbackSkipped=1` | **03:35** 已是当天快照 → `stageChanged=1`，`rollbackSkipped=0` |

契约 03:00 前发完，今天赶上了。34 条对应 40 白名单里仍在催、数仓会发快照的案（已结清的不再发 `caseEvent`）。

### 4.2 repaymentEvent 2h 窗 — 旁证通过

8/28：`502131` 结清后从 13:30 刷到 23:00+（当时近 10 小时窗）。  
8/29：inbox 全日 **34 条全是 `caseEvent`**，`repaymentEvent=0`；`502131`/`527448`/`502356` 当日 inbox 也是 0。已结清案不再被 15 分钟任务反复投。今日没有新的结清样本，不能数「同一笔最多几条」，但「不再铺一整天」成立。

---

## 5. 催收库表

| 表 | 8/29 22:10 |
|---|---|
| `t_ai_collection` | 41（仍是原 Pilot 圈） |
| `t_ai_collection_inbox` 当日 | 34，全 `caseEvent`，03:00 |
| 悬挂 `EXECUTING` | 0 |
| `t_event_outbox` PENDING | 0 |
| `t_event_dlq` 当日 | 0 |
| 启动 ERROR | 0 |

timeline 当日 OUT：SMS 32、PUSH 32、AI_CALL 56（09:15 的 32 + 14:30 的 24）。

---

## 6. 问题清单

| 项 | 挡触达？ | 状态 |
|---|---|---|
| 200 案白名单未切换 | **挡了 8/29 当天分流** | **23:12 已切**：两份名单各 200、互为全集、与 CSV 一致；health 200。首跑日是 **8/30** |
| 话术档跟旧 snapshot.stage | 对客措辞 | 催收代码仍未改；今日未抽正文 |
| 取消计划残留 PENDING | 否 | 已还款 / 升档旧计划，扫描不捞 |

---

## 7. 明天（8/30，200 案首跑）

**23:12 PHT 已切名单并重启**，不是发版。

| 项 | 值 |
|---|---|
| 备份 | `/opt/app/pilot.env.bak.20260829151248` |
| `COLLECTION_PILOT_LOAN_IDS` | **200**，与 [e2e200 CSV](./e2e200_loan_ids_20260829.csv) 一致，样例 `469165,468179,468279` |
| `COLLECTION_SCAN_CASE_IDS` | **200**，与接入名单交集 **200** |
| 容器 | `collection-admin` Up，StartedAt `2026-08-29T15:12:59Z`，loopback health **200** |
| 启动 | 无 ERROR、无空名单拒启；`sms-test-mode=true` 警告仍在（与 8/27 相同，未改） |

原 40 案已不在扫描名单，8/30 不再捞他们的步骤（库里计划行还在，属预期）。

8/30 实绩见 [8/30 自动跑记录](./MOCASA催收系统升级_Phase1_自动跑记录_20260830.md)：**进 143 / 200**，五槽按时；S0 整桶未发；14:30 悬挂 2；还款 2h 窗未守住。
