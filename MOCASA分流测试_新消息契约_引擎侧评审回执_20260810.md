# MOCASA 催收系统升级 Phase1 — 新消息契约 引擎侧评审回执

**日期**：2026-08-10
**评审对象**：`MOCASA分流测试_新老字段对齐清单.md`（新系统消息契约设计，与数据 / 接入团队对齐稿）
**评审方**：新催收系统主架构 / 引擎侧
**收件方**：运维同事、数据同事（第一部分）；接入 / 引擎开发（第二部分）
**结论**：**方向认可，当前版本尚不满足运行条件。** 6 项阻断（P0）必须先改，12 项对齐（P1）可与联调并行。P0-1 若按契约字面实现，会造成"已结清用户继续被催、仍逾期用户被停催"的合规级事故。

## 如何使用本回执

本文分两部分，各自独立、互不依赖：

| 部分 | 内容 | 收件方 | 需要的动作 |
| --- | --- | --- | --- |
| **第一部分**（§2–§6） | PubSub 消息格式必须满足的条件、验证 SQL、待确认问题 | **数据同事、运维同事** | 改契约与取数逻辑、跑 10 条 SQL、回复 15 个问题 |
| **第二部分**（§7–§8） | 引擎 / 接入 / 契约层的配套改动 | 引擎侧（我方） | 无需数据 / 运维参与，仅供知情与排期对齐 |

**只关心"消息格式要满足什么"的同事，直接看 §3 验收清单 + 附录 A 样例即可**；§2 是每条要求的理由与后果，§5 是需要先跑的数据验证。

---

## 目录

1. [结论摘要](#1-结论摘要)

**第一部分：数据 / 运维同事需要完成的**

2. [P0 阻断项（消息格式必须满足）](#2-p0-阻断项消息格式必须满足)
3. [PubSub 消息格式验收清单](#3-pubsub-消息格式验收清单)
4. [P1 对齐项（可与联调并行）](#4-p1-对齐项可与联调并行)
5. [请数据同事先跑的验证 SQL](#5-请数据同事先跑的验证-sql)
6. [待确认问题清单（请逐条回复）](#6-待确认问题清单请逐条回复)

**第二部分：引擎侧负责对接的**

7. [引擎侧承接的改动](#7-引擎侧承接的改动无需数据--运维参与)
8. [引擎侧字段落地缺口与精简建议](#8-引擎侧字段落地缺口与精简建议)

**附录**

- [附录 A：修正后的 payload 样例](#附录-a修正后的-payload-样例)
- [附录 B：参考依据](#附录-b参考依据)

---

## 1. 结论摘要

先肯定三点做对了的地方，这些不需要改：

1. `caseId = loan_id` 的口径正确，与领域模型 §4.1「`caseId` 全链路等同 `loan_id`，**不是**旧库 hex 行主键 `t_collection.id`」完全一致；`userId` 也已具备，跨案件合规频控与还款去重键都够用。
2. 用 `t_loan_repayment_plan.is_loan_clear` 作结清权威信号（`MAX(is_loan_clear)=0` 判整笔结清）成立，且比老库 `repayment_status` / `STATUS` 可靠得多；已查实 `repayment_status` 全表恒为 2、无区分度而决定不引入，这个判断正确。
3. 字段名统一 camelCase、不做老字段名兼容、每期明细只认 `t_loan_repayment_plan` 为 SSOT，这三条与新系统契约层设计一致。

问题分布：

| 级别 | 数量 | 含义 | 是否阻断分流测试 |
| --- | --- | --- | --- |
| P0 | 6 | 语义错误 / 口径错误 / 幂等失效，跑起来会出错 | **是** |
| P1 | 12 | 命名、映射、归属、边界定义未收口 | 否，可与联调并行 |
| 待验证 | 10 条 SQL | 结论依赖真实数据，不凭猜 | 影响 P0-3 / P0-6 的定级与参数 |

---

# 第一部分：数据 / 运维同事需要完成的

## 2. P0 阻断项（消息格式必须满足）

### P0-1　还款事件的类型语义与引擎完全相反

**契约现状**（对齐清单 §6.2 字段表、§7.7 表）：

```text
eventType = isFullCleared ? CASE_BALANCE_UPDATED : REPAYMENT_RECEIVED

REPAYMENT_RECEIVED     ← isFullCleared=false（部分还款）→ 刷新 repayPlan[]、继续催收
CASE_BALANCE_UPDATED   ← isFullCleared=true （整笔结清）→ 取消计划，切状态 SETTLED
```

**引擎实际语义**（领域模型 §6.2、接入规格 §2.2.2，且已在代码中固化）：

| EventType | 引擎动作 |
| --- | --- |
| `REPAYMENT_RECEIVED` | **整笔 loan 全额结清** → 取消该案件全部活跃计划，`cancel_reason=REPAID`，撤销已注册 Job |
| `CASE_BALANCE_UPDATED` | **部分还款** → 仅刷新活跃计划快照的 `totalOutstanding`，不改状态、不改话术、不取消 |

代码位置：`collection-ingestion/.../IngestionService.java` 的 `repayment()` / `balanceUpdated()`；`collection-engine/.../PlanLifecycleManager.java` 的 `onRepaymentReceived()` / `onCaseBalanceUpdated()`；枚举注释见 `collection-common/.../enums/EventType.java`。

**按契约字面实现的后果**：

- 客户还了一期、整笔仍逾期 → 引擎收到 `REPAYMENT_RECEIVED` → **全量停催**，剩余期数再也不催。
- 客户整笔结清 → 引擎收到 `CASE_BALANCE_UPDATED` → **只改金额、计划继续跑**，已结清客户继续收催收短信、继续被 AI 外呼。

**请二选一**（我方推荐方案 B）：

- **方案 A**：契约把映射调回来 —— 整笔结清发 `REPAYMENT_RECEIVED`，部分还款发 `CASE_BALANCE_UPDATED`。改动最小，但两边共用同名枚举，未来仍有再次误解的风险。
- **方案 B（推荐）**：适配层不使用这两个内部事件名。`repaymentEvent` 统一一个 `eventType: "REPAYMENT"`，只如实携带 `isFullCleared` 布尔量，由接入层按引擎语义分流为内部事件。适配层从此不需要知道引擎内部事件名，也不会再被同名不同义坑到。

> 结清判定的**信号选择**（`MAX(is_loan_clear)=0`）我方认可，无需更改；本项只改事件类型的表达方式。

---

### P0-2　对客金额口径含未到期期数，会把应还金额说高

**契约现状**（§5.2、§6.2）：

- `overdueAmount = SUM(principal+interest)` 未结清期 —— 无"已到期"过滤
- `remainingAmount = SUM(principal+interest+penalty_interest)` where `is_loan_clear != 0` —— 同样无过滤
- `totalOutstanding = overdueAmount + penaltyAmount`

**规格要求**（渠道编排规格 §6.3.1；`README_ContextSnapshot契约对齐` 金额 SSOT 表）：

> 应还 `{amount}`：**已到期且未结清合计**；未到期不计（**无加速条款**）。period3 未到期不算入。
> `amount_due` 的 SSOT = `caseContext.totalOutstanding`；`overdue_days` = `caseContext.dpd`。

`{amount}` / `amount_due` 是 SMS / Push / Email 三类模板唯一的金额变量，直接印在对客文案里（`PHP {amount}` / `₱ {{amount_due}}`）。

**契约自带样例即已踩中**：`loan_id=525441`，三期各 3000，`dpd=-2`（期 1 于 2026-08-11 到期，当日尚无任何期到期），而 `totalOutstanding=9000.0`。这条 S0 提醒会对客户说 **PHP 9,000.00**，而客户当期应还 **3,000.00**。菲律宾市场对催收文案金额准确性敏感，这属于不可上线项。

本项需同时改三处：

**(A) 加"已到期"过滤，两个口径拆成两个字段**

| 字段 | 口径 | 用途 |
| --- | --- | --- |
| `totalOutstanding` | **已到期且未结清**（本息 + 罚息），即 `due_date <= as_of AND is_loan_clear != 0` | **对客渲染唯一来源**，进 `{amount}` / `amount_due` |
| `remainingAmount` | 全部未结清（含未到期） | 仅对账 / 报表，不进文案 |

**(B) 部分结清期（`is_loan_clear=2`）必须扣减已还部分**

`is_loan_clear=2` 计入 `remainingPeriods` 是对的，但金额汇总对该期取的是**整期** `principal+interest+penalty_interest`，未扣已还部分 → 剩余金额虚高。需要该期残值来源：`t_loan_repayment_plan` 是否有已还金额列，或用 `t_loan_repayment.real_amount` 按期累计扣减。**请先跑 SQL-4 确认列是否存在**，再定采哪种算法。

**(C) 配套项：三期产品会出现"已到期部分为 0"的状态**

改成"已到期"口径后，3 期产品会出现「整笔未结清，但当日已到期部分为 0」（当期已还、后续期未到期）。引擎当前只拦截负金额、**零金额会被正常接受**：

`PlanLifecycleManager.java` `onCaseBalanceUpdated()` L177–180：

```java
if (caseId == null || amount == null || amount.signum() < 0) {
    log.warn("[balanceUpdated] ignore invalid event case={} amount={}", caseId, amount);
    return noEvents();
}
```

即金额 0 会被写入快照，后续步骤会渲染出「PHP 0.00」的催收文案。该状态下案件本就不应被催收，处置需双方各出一半：

- **数据侧**：`isFullCleared=false` 且已到期未结清为 0 时，除了发 `repaymentEvent`，还需发一条 `CASE_STAGE_CHANGED`，`dpd` 按**下一期 due_date** 重算（通常回落到 S0 或负 dpd）。这也正是 P0-3 要求"DPD 可下降"的直接用例。
- **引擎侧（我方承担，见 §7）**：`totalOutstanding = 0` 时步骤执行前置检查直接跳过触达，不发零金额文案。

---

### P0-3　`dpd = MAX(overdue_days)` 未过滤已结清期，DPD 会只涨不跌

**契约现状**（§7.1）：

```sql
-- 该 loan 全部期：取 overdue_days 最大值
dpd = MAX(overdue_days)
```

**规格要求**（接入规格 §4.3）：切量后 Max DPD 取**未结清且在催** bill 的 `max(bill_dpd)`，在催扫描条件为 `full_repay_time IS NULL AND total_not_paid > 0`；且 **Max DPD 下降时必须发降阶 `STAGE_CHANGED`**（阶段可回退）。

**风险链条**：若 `overdue_days` 在期次结清后保留历史值（这是我们判断的高概率情况，因为它是批量算出后落库的列，而非实时计算），那么"期 1 曾逾期 32 天且已还清、期 2 尚未到期"的 3 期账户会持续上报 `dpd=32`：

1. 案件常驻 S4，阶段永不回退 → 接入规格 §4.3 的降阶要求实质失效；
2. 期 2 的 D-3 / D0 到期前提醒（S0）永远不会触发 → 少催；
3. 已结清期的客户持续收到"您已逾期 32 天"的文案 → 误催 + 文案事实错误。

**请改**：

```sql
-- 建议口径：只对未结清期取 Max，保留未到期期次以便算出 S0 的负数 dpd
dpd = MAX(overdue_days) WHERE is_loan_clear != 0
```

**注意不要写成** `WHERE due_date <= as_of`：那样 S0（D-3 ~ D0，尚无到期期次）会取不到行、算不出负数 dpd，S0 提醒整档失效。

**定级依赖验证**：此项严重程度取决于"已结清期的 `overdue_days` 是否被清零"，请先跑 SQL-1 与 SQL-2，我们据结果确认是纠正口径还是仅补一条防御。

---

### P0-4　消息信封与关联键：缺消息级唯一 ID、消息时间与路由键

`caseId = loan_id` 的选择本身正确（见 §1），但消息层的关联与去重要素不完整，以下四项都需要补。

**(A) `caseEvent` 缺 `messageId` 与消息时间 → 幂等 / 乱序 / 迟到三道闸门全部失效**

接入规格 §3.3 对 `case_push` 定的是依次三道检查：

| 检查 | Redis key | TTL | 命中处置 | 依赖字段 |
| --- | --- | --- | --- | --- |
| 同条消息重投 | `collection:ingestion:dedup:msg:{message_id}` | 7d | ack 跳过 | **`message_id`** |
| 同 loan 乱序（更旧消息） | `collection:ingestion:last-seen:{loan_id}` | 90d | ack 跳过 | **消息时间水位** |
| 同催收周期重复入案 | `collection:ingestion:ingested:{loan_id}` | 90d | ack 跳过 | `loan_id` |

另有迟到规则：消息时间超 24h 的 `case_push` 需走核验后受控 replay，其余 ack 跳过。

`caseEvent` 既无消息级唯一 ID、也无任何时间戳 → 前两道闸门无法工作。Pub/Sub 是 at-least-once 投递，重投必然发生，届时会**重复入案、重复建计划、重复触达**。`repaymentEvent` 有 `messageId`（`t_loan_repayment.id`）与 `userId`，去重键 `dedup:msg:{user_id}:{message_id}` 可以拼出，但同样缺时间水位。

**请改**：两类消息统一补 `messageId`（消息级唯一，非业务主键）与 `occurredAt`（ISO 8601 带时区）。若适配层不便自造，可明确约定"使用 Pub/Sub 原生 `messageId` 与 `publishTime`"，我方接入层照此实现——但需要书面确认，不能靠默认。

**(B) 路由键缺失**

当前接入层按 Pub/Sub **attribute `dataType`**（`case_push` / `repayment_push_and_load`）分流，新契约只有 body 里的 `eventType`。请二选一并书面确认：适配层在 attribute 里带 `dataType`，或由我方改接入层改为按 body `eventType` 路由（改动在我方模块，可承接，但需先定）。

**(C) `caseId` 的类型与规范化**

`t_loan_repayment.loan_id` 是 STRING、`t_loan_repayment_plan.loan_id` 是 INT64。cast 必须规范化（去首尾空格、无前导零、纯数字），否则两类消息会指向不同 key，`caseEvent` 与 `repaymentEvent` 关联不上。引擎侧 `caseId` / `userId` 内部类型为 `Long`，纯数字字符串可解析，请在契约中写明"数字字符串（可安全转 Long）"。**请跑 SQL-8 确认脏值量。**

**(D) `caseId` 必须与分流切片、老系统对账同源**

`caseId` 必须与路由层分流切片所用的 `hash(loan_id)`、以及老系统 `t_collection.loan_id` 是**同一个值**，否则并行期对账对不上、灰度名单与实际触达集合不一致。

**(E) `messageId` 唯一性待确认**

`repaymentEvent.messageId` 取 `t_loan_repayment.id` 作去重键是合理的，但需确认它全局唯一而非按 loan 自增（**SQL-7**）。若是按 loan 自增，去重键需改为 `{loan_id}:{id}` 组合。

---

### P0-5　`CASE_STAGE_CHANGED` 只带 4 个字段，与"引擎不回库"前提冲突

**契约现状**（§5.2 末尾）：`CASE_STAGE_CHANGED` 必填 `caseId` + `stage` + `dpd` + `maxDpd`；`CASE_CEASED` 必填 `caseId` + `maxDpd`。

**引擎实际行为**：阶段变更时引擎**不解析事件里的快照字段**，只做 carry-forward —— 从该案件旧的活跃计划上取已冻结快照、刷新 stage 后建新计划；**旧计划不存在时降级读库**（`CaseService`）。

`PlanLifecycleManager.java` `onStageChanged()` L92–100：

```java
// 决策 B：carry-forward 旧计划已冻结快照（同案件无新 case_push），刷新 stage；
// 取消前先取快照。缺失时 createPlanForStage 降级 CaseService。
ContextSnapshot carried = null;
```

**冲突点**：只要该案件此刻没有活跃计划（上一个计划已完成 / 已穷尽 / 曾被取消），carry-forward 取不到快照，就会走降级读库路径。而新契约的前提是"旧库表仅作为取数来源，引擎侧不回库"——这条路径拿不到姓名、手机号、邮箱、推送 token 与金额，新阶段的计划会带着空快照建起来，触达渲染必然失败。

**请改**：`CASE_STAGE_CHANGED` 与 `CASE_INGESTED` 使用**同一份全量 payload**（契约 §4 已把 `caseEvent` 定义为"案件级生命周期快照"，与此一致）。`CASE_CEASED` 只做停催、不建计划，保持 `caseId` + `maxDpd` 即可。

---

### P0-6　还款事件的触发时机存在读写竞态，会把整笔结清误判为部分还款

**契约现状**：§7.6 已记录 `clear_time` 可晚于还款单 `trade_finish_time` 约 1 秒（贷款 `52632` 第 2 笔），而 §7.7 要求"每笔 `t_loan_repayment` 新增时**立即**同步计算 `isFullCleared` / `remainingPeriods` / `remainingAmount`"。

**冲突点**：契约只写了"适配层须在该笔还款对应的 `is_loan_clear` 落库后再计算"，但没有给出任何保障机制。竞态发生时，整笔结清会被算成 `isFullCleared=false` → 引擎不停催 → 已结清客户被多催一轮（按 plan 槽位，最坏是一次 AI 外呼 + 一条短信）。

**请改**：把"落库后再算"落成可执行规则，三选一并写入契约：

1. 延迟 N 秒触发计算（N 依 SQL-5 的 p99 取值，建议再留 2 倍余量）；
2. 读到 `is_loan_clear` 未更新时按固定间隔重试 M 次，超次数告警并按当前值发出；
3. 与账务落库同事务 / 同批次，从根上消除竞态（最优，但依赖账务侧配合）。

**请先跑 SQL-5 量化时差分布**再定 N / M。

---

## 3. PubSub 消息格式验收清单

本节是 §2 全部要求的字段级落地形式。**适配层只要满足这两张表，消息格式即算通过引擎侧验收。**（标注"待定"的字段依赖 §6 对应问题的回复。）

### 3.1 消息级约束（非字段）

| 项 | 要求 | 来源 |
| --- | --- | --- |
| Topic | 新系统独立 topic，与老系统推送隔离 | 契约 §1，已满足 |
| 路由键 | Pub/Sub attribute `dataType`，或改由 body `eventType` 路由（二选一，待定） | P0-4(B)、Q7 |
| 投递保证 | at-least-once；重投由 `messageId` 去重，适配层无需保证 exactly-once | P0-4(A) |
| 消息完整性 | 一次案件入案 = 一条完整 `caseEvent`，无多消息关联、无缓冲等待 | 契约 §1，已满足 |
| 负金额 | 负 `real_amount`（退款 / 冲正）**不得**作为普通还款事件推送 | P1-12、Q9 |
| 还款单范围 | 只推**成功**还款单（失败 / 挂起 / 待清算不进 topic） | Q8 |
| 计算时机 | `is_loan_clear` 落库后再计算还款事件字段 | P0-6、Q10 |
| 日切触发 | `CASE_STAGE_CHANGED`（含降阶）/ `CASE_CEASED` 的扫描频率与时点（0:35 PHT）及归属待定 | P1-8、Q11 |

### 3.2 `caseEvent` 字段清单

适用 `CASE_INGESTED` 与 `CASE_STAGE_CHANGED`（两者共用同一份全量 payload，见 P0-5）。

| 字段 | 类型 | 必填 | 口径 / 格式约束 | 关联 |
| --- | --- | --- | --- | --- |
| `eventType` | String | 是 | `CASE_INGESTED` / `CASE_STAGE_CHANGED` / `CASE_CEASED` | — |
| `messageId` | String | **是（新增）** | 消息级唯一，非业务主键；重投保持同值 | P0-4(A) |
| `occurredAt` | String | **是（新增）** | ISO 8601 带时区，如 `2026-08-09T00:35:12+08:00` | P0-4(A) |
| `caseId` | String | 是 | `= loan_id`；纯数字字符串，无前导零 / 空格，可安全转 Long；与分流切片、老库 `t_collection.loan_id` 同值 | P0-4(C)(D) |
| `userId` | String | 是 | 纯数字字符串，规范化同上 | P0-4(C) |
| `product` | String | 是 | `t_loan.product_id` 原始值；分期映射另行下发配置 | P1-2 |
| `stage` | String | 是 | `S0`~`S4`；`dpd >= 91` 时不发 `stage`（改发 `CASE_CEASED`） | 已一致 |
| `dpd` | Integer | 是 | **`MAX(overdue_days) WHERE is_loan_clear != 0`**（须可下降） | **P0-3** |
| `maxDpd` | Integer | 是 | 同 `dpd`；`CASE_CEASED` 默认 91 | 已一致 |
| `collectionStatus` | String | 是 | `IN_COLLECTION` / `SETTLED` / `CEASED`（原名 `caseStatus`，建议改名） | P1-1、Q14 |
| `totalOutstanding` | Decimal | 是 | **已到期且未结清**（本息 + 罚息）；部分结清期须扣已还部分；**对客文案唯一来源** | **P0-2(A)(B)** |
| `penaltyAmount` | Decimal | 是 | 已到期未结清期的罚息合计 | P0-2(A) |
| `remainingAmount` | Decimal | 否 | 全部未结清（含未到期），**仅对账，不进文案** | P0-2(A) |
| `dueDate` | Date | 是 | `MIN(due_date)` 未结清期 | 已一致 |
| `borrower.name` | String | 是 | SMS / Email / Push 的 `{name}` | 已一致 |
| `borrower.phone` | String | 是 | E.164，`+63` 前缀 | 已一致 |
| `borrower.email` | String | 否 | 缺失则 Email 渠道 `COMPLIANCE_BLOCKED`，不影响其他渠道 | 已一致 |
| `borrower.language` | String | 是 | Phase 1 固定 `en` | 已一致 |
| `device.pushToken` | String | 否 | 缺失时 PUSH 自动降级 SMS，可接受 | 已一致 |
| ~~`overdueAmount`~~ | — | 否 | 建议移除，与 `totalOutstanding` 冗余 | §8 |
| ~~`repayPlan[]`~~ | — | 否 | 建议移除，仅作适配层内部计算依据 | P1-9 |
| ~~`isFirstLoan` / `payCount`~~ | — | 否 | Phase 1 引擎无落地键，建议移除 | §8、Q13 |
| ~~`everReachedS2Overdue` / `concurrentOverdueBills`~~ | — | 否 | FIRM 接线时才需要，Phase 1 建议移除 | §8、P1-3/4 |

`CASE_CEASED` 例外：仅需 `eventType` + `messageId` + `occurredAt` + `caseId` + `maxDpd`。

### 3.3 `repaymentEvent` 字段清单

| 字段 | 类型 | 必填 | 口径 / 格式约束 | 关联 |
| --- | --- | --- | --- | --- |
| `eventType` | String | 是 | 方案 B：固定 `REPAYMENT`；方案 A：按引擎语义取 `REPAYMENT_RECEIVED`（结清）/ `CASE_BALANCE_UPDATED`（部分） | **P0-1**、Q1 |
| `messageId` | String | 是 | `t_loan_repayment.id`；须确认全局唯一，否则改 `{loanId}:{id}` | P0-4(E)、SQL-7 |
| `occurredAt` | String | **是（新增）** | ISO 8601 带时区 | P0-4(A) |
| `caseId` | String | 是 | 规范化同 `caseEvent`（`t_loan_repayment.loan_id` 是 STRING，须 trim + 去前导零） | P0-4(C) |
| `userId` | String | 是 | 还款去重键 `dedup:msg:{user_id}:{message_id}` 的组成部分 | P0-4(A) |
| `repayTime` | DateTime | 是 | `trade_finish_time` | 已一致 |
| `paidAmount` | Decimal | 是 | 本笔实收净额，不含支付手续费 | 已一致 |
| `isFullCleared` | Boolean | 是 | `MAX(is_loan_clear) = 0`；**须在 `is_loan_clear` 落库后计算** | P0-1、**P0-6** |
| `totalPeriods` | Integer | 是 | 该 loan 总期数 | 已一致 |
| `remainingPeriods` | Integer | 是 | `COUNT(is_loan_clear != 0)` | 已一致 |
| `totalOutstanding` | Decimal | **是（新增）** | **已到期且未结清**（含罚息，扣减部分结清期已还部分）；引擎据此刷新对客金额 | **P0-2(A)(B)** |
| `remainingAmount` | Decimal | 否 | 全部未结清（含未到期），仅对账 | P0-2(A) |
| `product` | String | 是 | 同 `caseEvent` | P1-2 |

---

## 4. P1 对齐项（可与联调并行）

| # | 项 | 现状 | 建议 | 责任方 |
| --- | --- | --- | --- | --- |
| 1 | `caseStatus` vs `collectionStatus` | 契约用 `caseStatus ∈ {IN_COLLECTION, SETTLED, CEASED}`；引擎的拒建计划依据是 `collectionStatus=CEASED`（`dpd>=91`），而 `caseStatus` 在领域模型里另有含义（案件业务状态） | 契约直接改名 `collectionStatus` 避免同名不同义；或明确映射由我方做 | 数据侧改名 or 引擎侧加映射 |
| 2 | `product` 语义变更 | 由原 `appName`（品牌）改为数字 `product_id`（1~7） | 需把「`{3,4}` = 3 期」映射下发至引擎 / 渠道配置（Nacos），否则 FIRM 的"三期并发逾期"条件无从判断；同时确认无任何按品牌选模板的逻辑依赖 `appName` | 引擎侧配置，数据侧确认映射稳定性（SQL-9） |
| 3 | `everReachedS2Overdue` 定义偏差 | 契约：该用户全部 loan `MAX(overdue_days) >= 4` | 渠道规格 §6.3.1 要求**排除本笔的首次逾期波次**，否则几乎所有 S2+ 案件都会被判"难催"。Phase 1 tone 固定 `STANDARD` 故不阻断，但 FIRM 接线前必须修 | 数据侧 |
| 4 | `concurrentOverdueBills` 定义偏差 | 契约 §7.5 用 `clear_time IS NULL OR clear_time > due_date` | 该条件把"当时逾期、现已还清"的 bill 也计入，与"并发（当前同时）逾期"语义不符，且与契约 §7.6 自己写的"结清判定不使用 `clear_time` 比较"自相矛盾。建议改为 `is_loan_clear != 0 AND due_date <= as_of`；并确认统计范围是"该用户全部 loan"还是渠道规格所指的"同一笔三期 loan 内" | 数据侧 |
| 5 | `repayUrl` 双 SSOT | 契约固定短链 `https://mocasa.com/s/4cTu`（模板硬编码）；引擎按模板生成 `https://app.mocasa.com/repay/{caseId}` | 固定短链带不了 `caseId`，Push 的 `data.deep_link` 归因会丢失、无法按案件统计点击。请确认是否接受归因缺失；若不接受，改用带 `caseId` 的深链 | 双方确认 |
| 6 | `payCount` 语义 | 取 `t_loan.user_loan_rank_total` | 该列是**贷款笔数**，领域模型 §4.1 的 `payCount` 是**历史还款次数**。Phase 1 未用于文案，可暂缓，但请在契约中注明实际语义避免后续误用 | 数据侧注明 |
| 7 | 时区表述 | §7.1 用 `CURRENT_DATE('Asia/Shanghai')` | PHT 与 CST 同为 UTC+8 且无 DST，**日期结果一致、无功能差异**；但建议统一写 `Asia/Manila`，避免后续市场扩展时踩坑 | 数据侧 |
| 8 | 日切归属未定义 | 契约未写 `CASE_STAGE_CHANGED` / `CASE_CEASED` 的触发频率与时点，也未提降阶 | 接入规格定的是每日 **0:35 PHT**（账务落库 ≥30min 后）扫描在催案件、阶段变化即发（含降阶）、`dpd>=91` 发停催。请明确该扫描由适配层承担还是仍由我方接入层承担 | 双方确认 |
| 9 | `repayPlan[]` 无落地处 | 契约在 `caseEvent` 携带每期明细 | 引擎 `ContextSnapshot` 无分期结构，无处存放。建议仅作适配层内部计算依据、不进 payload；改为直接给出算好的 `totalOutstanding` / `remainingAmount` / `dueDate` | 数据侧 |
| 10 | S4 上界 | 契约 `computeStage` 写 `dpd >= 31 → S4 (D+31~90)` | 与规格一致（≥91 无 Stage、进完全停催），仅确认 `dpd < -3` 归 S0 且"无提醒"由引擎 / 渠道侧不排步骤实现，而非由消息侧过滤 | 已一致，确认即可 |
| 11 | `loanAmount` 缺失 | 领域模型 §4.1 中 `loanAmount` 标必填 | Phase 1 引擎与全部模板均未使用，可暂不补；若后续文案需要再加 | 暂缓 |
| 12 | 退款冲正无复催路径 | 契约 §7.7 指出部分退款以负 `real_amount` 表现（min = -3.0）。接入规格 §3.2 对非结清还款的负金额是 **ack + poison/DLQ 且不发布**；"已结清后被冲正"的案件在新契约里没有任何复催入口 | 请明确负 `real_amount` 是否会出现在推送流中；若会，冲正导致 loan 由结清转未结清时是否重发 `CASE_INGESTED`（SQL-6） | 运维 + 数据 |

---

## 5. 请数据同事先跑的验证 SQL

以下 10 条用于把 P0-2 / P0-3 / P0-6 的结论从"推断"变成"已验证"。BigQuery 语法，`as_of` 统一用 `CURRENT_DATE('Asia/Manila')`。

**SQL-1　已结清期的 `overdue_days` 是否被清零（决定 P0-3 定级）**

```sql
SELECT
  is_loan_clear,
  COUNT(*) AS cnt,
  MIN(overdue_days) AS min_od,
  MAX(overdue_days) AS max_od,
  COUNTIF(overdue_days > 0) AS positive_od
FROM detail.t_loan_repayment_plan
WHERE clear_time IS NOT NULL AND clear_time > due_date   -- 曾逾期且已结清
GROUP BY is_loan_clear;
```

> 判读：若 `is_loan_clear=0` 组的 `positive_od > 0`，说明 `overdue_days` 冻结历史值，P0-3 成立且必须修。

**SQL-2　两种 dpd 口径的差异案件量**

```sql
WITH p AS (
  SELECT
    loan_id,
    MAX(overdue_days) AS dpd_all,
    MAX(CASE WHEN is_loan_clear != 0 THEN overdue_days END) AS dpd_unpaid
  FROM detail.t_loan_repayment_plan
  GROUP BY loan_id
)
SELECT
  COUNT(*) AS loans,
  COUNTIF(dpd_all != dpd_unpaid) AS diff_loans,
  COUNTIF(dpd_unpaid IS NULL) AS fully_cleared_loans,
  COUNTIF(dpd_all >= 31 AND dpd_unpaid < 4) AS would_be_miscollected  -- 实际不逾期却被判 S4
FROM p;
```

**SQL-3　"已到期未结清"与"全部未结清"金额差异（量化 P0-2 影响面）**

```sql
WITH p AS (
  SELECT
    loan_id,
    SUM(CASE WHEN is_loan_clear != 0 AND due_date <= CURRENT_DATE('Asia/Manila')
             THEN principal + interest + penalty_interest END) AS due_outstanding,
    SUM(CASE WHEN is_loan_clear != 0
             THEN principal + interest + penalty_interest END) AS remaining_all
  FROM detail.t_loan_repayment_plan
  GROUP BY loan_id
)
SELECT
  COUNT(*) AS loans,
  COUNTIF(remaining_all > IFNULL(due_outstanding, 0)) AS inflated_loans,
  AVG(SAFE_DIVIDE(remaining_all, due_outstanding)) AS avg_inflation_ratio
FROM p
WHERE remaining_all IS NOT NULL;
```

**SQL-4　`t_loan_repayment_plan` 是否有"该期已还金额"列（决定 P0-2(B) 怎么修）**

```sql
SELECT column_name, data_type
FROM detail.INFORMATION_SCHEMA.COLUMNS
WHERE table_name = 't_loan_repayment_plan'
ORDER BY ordinal_position;
```

**SQL-5　`clear_time` 相对 `trade_finish_time` 的落库时差分布（决定 P0-6 竞态保护参数）**

```sql
WITH r AS (
  SELECT CAST(loan_id AS INT64) AS loan_id, trade_finish_time
  FROM detail.t_loan_repayment
  WHERE trade_finish_time IS NOT NULL
)
SELECT
  APPROX_QUANTILES(TIMESTAMP_DIFF(p.clear_time, r.trade_finish_time, SECOND), 100)[OFFSET(50)] AS p50_sec,
  APPROX_QUANTILES(TIMESTAMP_DIFF(p.clear_time, r.trade_finish_time, SECOND), 100)[OFFSET(95)] AS p95_sec,
  APPROX_QUANTILES(TIMESTAMP_DIFF(p.clear_time, r.trade_finish_time, SECOND), 100)[OFFSET(99)] AS p99_sec,
  MAX(TIMESTAMP_DIFF(p.clear_time, r.trade_finish_time, SECOND)) AS max_sec
FROM detail.t_loan_repayment_plan p
JOIN r ON r.loan_id = p.loan_id
WHERE p.clear_time IS NOT NULL
  AND ABS(TIMESTAMP_DIFF(p.clear_time, r.trade_finish_time, SECOND)) <= 3600;
```

**SQL-6　负 `real_amount`（退款 / 冲正）规模与影响**

```sql
SELECT
  COUNT(*) AS negative_rows,
  COUNT(DISTINCT loan_id) AS affected_loans,
  MIN(real_amount) AS min_amount,
  MIN(trade_finish_time) AS earliest,
  MAX(trade_finish_time) AS latest
FROM detail.t_loan_repayment
WHERE real_amount < 0;
```

> 追加确认（无法纯 SQL 判定）：冲正是否会把已 `is_loan_clear=0` 的期次改回非 0？若会，需要复催入口。

**SQL-7　`t_loan_repayment.id` 是否全局唯一（决定能否单独作去重键）**

```sql
SELECT
  COUNT(*) AS rows_total,
  COUNT(DISTINCT id) AS distinct_ids,
  COUNT(DISTINCT CONCAT(CAST(loan_id AS STRING), ':', CAST(id AS STRING))) AS distinct_loan_id_pairs
FROM detail.t_loan_repayment;
```

**SQL-8　`loan_id` 脏值（决定 cast 规范化规则）**

```sql
SELECT
  COUNTIF(NOT REGEXP_CONTAINS(loan_id, r'^[0-9]+$')) AS non_numeric,
  COUNTIF(REGEXP_CONTAINS(loan_id, r'^0[0-9]+$')) AS leading_zero,
  COUNTIF(loan_id != TRIM(loan_id)) AS with_space
FROM detail.t_loan_repayment;
```

**SQL-9　`product_id` 与实际期数的映射是否稳定（验证 `{3,4}` = 3 期）**

```sql
WITH periods AS (
  SELECT l.product_id, p.loan_id, COUNT(*) AS period_cnt
  FROM detail.t_loan_repayment_plan p
  JOIN detail.t_loan l ON l.loan_id = p.loan_id
  GROUP BY l.product_id, p.loan_id
)
SELECT product_id, period_cnt, COUNT(*) AS loans
FROM periods
GROUP BY product_id, period_cnt
ORDER BY product_id, period_cnt;
```

**SQL-10　`user_loan_rank_total` 语义确认（`payCount`）**

```sql
SELECT
  user_loan_rank_total,
  COUNT(*) AS users_or_loans,
  MIN(user_loan_rank) AS min_rank,
  MAX(user_loan_rank) AS max_rank
FROM detail.t_loan
GROUP BY user_loan_rank_total
ORDER BY user_loan_rank_total
LIMIT 50;
```

---

## 6. 待确认问题清单（请逐条回复）

| 编号 | 问题 | 回复方 | 关联 |
| --- | --- | --- | --- |
| Q1 | 采用方案 A（调回映射）还是方案 B（适配层只发 `isFullCleared`，由接入层分流）？ | 数据 + 接入 | P0-1 |
| Q2 | 金额是否改为"已到期且未结清"？是否接受拆 `totalOutstanding`（对客）/ `remainingAmount`（对账）两字段？ | 数据 | P0-2(A) |
| Q3 | 部分结清期（`is_loan_clear=2`）的已还部分怎么扣减？依 SQL-4 结果定 | 数据 | P0-2(B) |
| Q4 | SQL-1 结果如何？已结清期的 `overdue_days` 是否清零？ | 数据 | P0-3 |
| Q5 | 是否接受 `dpd = MAX(overdue_days) WHERE is_loan_clear != 0`？ | 数据 | P0-3 |
| Q6 | `messageId` / `occurredAt`：适配层自带，还是约定用 Pub/Sub 原生 `messageId` / `publishTime`？ | 运维 + 接入 | P0-4(A) |
| Q7 | 路由键：适配层在 Pub/Sub attribute 带 `dataType`，还是由我方改按 body `eventType` 路由？ | 运维 | P0-4(B) |
| Q8 | 上游是否保证只推送**成功**还款单（失败 / 挂起 / 待清算不进 topic）？ | 运维 | §3.1 |
| Q9 | 负 `real_amount` 冲正是否会出现在推送流中？冲正导致 loan 由结清转未结清时是否重发入案？ | 运维 + 数据 | P1-12 |
| Q10 | `is_loan_clear` 落库晚于还款单插入的竞态，采用延迟触发 / 重试探测 / 同事务？参数取多少（依 SQL-5）？ | 数据 + 接入 | P0-6 |
| Q11 | 日切扫描（`CASE_STAGE_CHANGED` 含降阶 / `CASE_CEASED`）由适配层承担，还是仍由我方接入层承担？时点是否为 0:35 PHT？ | 运维 + 接入 | P1-8 |
| Q12 | 是否同意 `CASE_STAGE_CHANGED` 与 `CASE_INGESTED` 共用全量 payload？ | 数据 | P0-5 |
| Q13 | Phase 1 是否可先精简掉 §3.2 表中划删除线的 6 类字段？ | 数据 | §8 |
| Q14 | `caseStatus` 是否改名 `collectionStatus`，或由我方做映射？ | 数据 | P1-1 |
| Q15 | 3 期产品「整笔未结清但当日已到期部分为 0」时，是否按 P0-2(C) 补发 `CASE_STAGE_CHANGED`（`dpd` 按下一期 due_date 重算）？ | 数据 | P0-2(C) |

---

# 第二部分：引擎侧负责对接的

以下内容**不需要数据 / 运维同事动手**，列出仅供知情与排期对齐。

## 7. 引擎侧承接的改动（无需数据 / 运维参与）

P0 项确认后，我方（引擎 / 接入 / 契约层）负责：

| # | 改动 | 触发条件 | 对应模块 |
| --- | --- | --- | --- |
| 1 | 按最终确认的路由键（attribute `dataType` 或 body `eventType`）分流两类消息 | Q7 回复后 | `collection-ingestion` |
| 2 | 按引擎语义把 `isFullCleared` 分流为内部事件（`REPAYMENT_RECEIVED` / `CASE_BALANCE_UPDATED`） | Q1 选方案 B | `collection-ingestion` |
| 3 | 字段扁平化映射：`borrower.*` → `name` / `phone` / `email` / `language`，`device.pushToken` → `jpushToken` | 无条件 | `collection-ingestion` |
| 4 | 把 `messageId` / `occurredAt` 接到三道去重键与乱序水位（`dedup:msg`、`last-seen`、`ingested`） | Q6 回复后 | `collection-ingestion` |
| 5 | 零金额防御：`totalOutstanding = 0` 时步骤前置检查跳过触达，杜绝「PHP 0.00」文案外发 | 无条件 | `collection-engine` |
| 6 | 停催防御保留：`dpd >= 91` 一律拒建计划，不依赖上游是否发 `CASE_CEASED` | 已实现，保留 | `collection-engine` |
| 7 | `{3,4}` = 3 期的产品分期映射下发至 Nacos，供 FIRM 判定与统计使用 | Q9(SQL-9) 确认后 | 配置 |
| 8 | 若确认需要 `isFirstLoan` / `payCount` / FIRM 两字段进 payload，扩 `collection-common` 事件键并全量回归 | Q13 回复"不精简"时 | `collection-common` |

> 第 8 项若触发，需同步通知渠道编排同事与持久化同事（契约层改动联动）。

## 8. 引擎侧字段落地缺口与精简建议

以下 7 个 `caseEvent` 字段在引擎当前的事件 payload 键集合中**没有落地键**。现有键仅：`caseId`、`userId`、`stage`、`dpd`、`maxDpd`、`product`、`totalOutstanding`、`penaltyAmount`、`dueDate`、`fullRepayTime`、`repayStatus`、`name`、`phone`、`email`、`language`、`jpushToken`、`cancelReason`、`cancelScope`。

| 字段 | 处置建议 |
| --- | --- |
| `overdueAmount` | 冗余（`totalOutstanding` 已含），建议不发 |
| `isFirstLoan` | 领域模型 `CaseContext` 有该字段，需我方扩契约层键；Phase 1 文案未用，建议先不发 |
| `payCount` | 同上 |
| `caseStatus` | 按 P1-1 收口后再定键名 |
| `repayPlan[]` | 建议不发（P1-9） |
| `everReachedS2Overdue` | FIRM 接线时才需要，Phase 1 建议先不发 |
| `concurrentOverdueBills` | 同上 |

**为什么建议精简**：扩 `collection-common` 契约层键属我方职责，但契约层改动牵一发动全身（需全量回归并同步通知渠道编排、持久化两位同事）。Phase 1 **只发引擎当前会消费的字段**，其余待 FIRM / Phase 2 一并接线，可少一次无收益的契约震动。请数据侧就此回复 Q13。

---

# 附录

## 附录 A：修正后的 payload 样例

以下样例已应用 P0-1 ~ P0-6 的建议改法，可直接作为适配层开发基线（仍以 §6 逐条回复确认为准）。

### A.1 `caseEvent`（`CASE_INGESTED` / `CASE_STAGE_CHANGED` 共用全量 payload）

以契约原样例 `loan_id=525441`（3 期、`dpd=-2`、仅期 1 于 2026-08-11 到期）为基准：

```json
{
  "eventType": "CASE_INGESTED",
  "messageId": "a7f3c1e0-9b21-4d5e-8f10-2c9d4b6a8e31",
  "occurredAt": "2026-08-09T00:35:12+08:00",

  "caseId": "525441",
  "userId": "2145521",
  "product": "3",
  "stage": "S0",
  "dpd": -2,
  "maxDpd": -2,
  "collectionStatus": "IN_COLLECTION",

  "totalOutstanding": 3000.0,
  "penaltyAmount": 0.0,
  "remainingAmount": 9000.0,
  "dueDate": "2026-08-11",

  "borrower": {
    "name": "CORA PAULINE AQUINO HIZON",
    "phone": "+639563093217",
    "email": "hizoncorapauline@gmail.com",
    "language": "en"
  },
  "device": {
    "pushToken": "161a3797c92cf7eaea6"
  }
}
```

关键差异说明：

| 字段 | 原契约 | 本样例 | 原因 |
| --- | --- | --- | --- |
| `messageId` / `occurredAt` | 无 | 新增 | P0-4：去重与乱序水位 |
| `totalOutstanding` | `9000.0`（全部未结清） | `3000.0`（**已到期未结清**） | P0-2：对客金额唯一来源 |
| `remainingAmount` | 无（原 `overdueAmount` 混用） | `9000.0`，仅对账用 | P0-2：两口径分离、不进文案 |
| `dpd` | `MAX(overdue_days)` 全部期 | `MAX(overdue_days) WHERE is_loan_clear != 0` | P0-3：DPD 需可下降 |
| `caseStatus` | `IN_COLLECTION` | 改名 `collectionStatus` | P1-1：与引擎拒建计划依据同名 |
| `repayPlan[]` / `overdueAmount` / `isFirstLoan` / `payCount` / FIRM 两字段 | 携带 | 移除 | §8：引擎当前无落地键，Phase 1 精简 |
| `CASE_STAGE_CHANGED` | 仅 4 字段 | 与本结构完全一致 | P0-5：引擎需全量快照建新计划 |

### A.2 `repaymentEvent`（单一类型，`isFullCleared` 表达结清）

部分还款（原样例贷款 `52632` 第 1 笔，还清期 1）：

```json
{
  "eventType": "REPAYMENT",
  "messageId": "435684",
  "occurredAt": "2024-05-24T16:27:43+08:00",

  "caseId": "52632",
  "userId": "2145521",
  "repayTime": "2024-05-24 16:27:43",
  "paidAmount": 827.0,

  "isFullCleared": false,
  "totalPeriods": 3,
  "remainingPeriods": 2,
  "totalOutstanding": 0.0,
  "remainingAmount": 1654.0,
  "product": "3"
}
```

整笔结清（第 2 笔，结清剩余 2 期）：

```json
{
  "eventType": "REPAYMENT",
  "messageId": "435685",
  "occurredAt": "2024-05-24T16:33:10+08:00",

  "caseId": "52632",
  "userId": "2145521",
  "repayTime": "2024-05-24 16:33:10",
  "paidAmount": 1654.0,

  "isFullCleared": true,
  "totalPeriods": 3,
  "remainingPeriods": 0,
  "totalOutstanding": 0.0,
  "remainingAmount": 0.0,
  "product": "3"
}
```

关键差异说明：

| 字段 | 原契约 | 本样例 | 原因 |
| --- | --- | --- | --- |
| `eventType` | `REPAYMENT_RECEIVED` / `CASE_BALANCE_UPDATED`（语义与引擎相反） | 统一 `REPAYMENT` | P0-1 方案 B：适配层不使用引擎内部事件名，分流由接入层做 |
| `isFullCleared` | 有 | 保留，成为唯一结清信号 | P0-1 |
| `totalOutstanding` | 无 | 新增 = **已到期且未结清**（含罚息），部分还款时驱动引擎刷新对客金额 | P0-2 |
| `remainingAmount` | 全部未结清（含未到期） | 保留，仅对账；且需扣减 `is_loan_clear=2` 期的已还部分 | P0-2(B) |
| `occurredAt` | 无 | 新增 | P0-4：乱序水位 |
| 计算时机 | "还款单新增时立即算" | 需按 SQL-5 结果加竞态保护 | P0-6 |

> 上例中部分还款的 `totalOutstanding=0.0`：期 2、期 3 在 2024-05-24 尚未到期，故"已到期未结清"为 0。该案件当日本就不应被催收，这正是"已到期"口径要达到的效果——但需按 **P0-2(C)** 同时补发 `CASE_STAGE_CHANGED`（`dpd` 按期 2 的 due_date 重算），且我方会在步骤前置检查里拦掉零金额触达，两侧都做才闭合（见 Q15）。
>
> 请数据同事重点核对这一条是否符合业务预期；若业务确实希望三期产品按整笔余额催收，则需先修订渠道编排规格 §6.3.1 的"无加速条款"约定，而不是在消息层绕过。

## 附录 B：参考依据

本回执的每条判断均对应以下文档或代码，便于双方复核：

- 事件语义与 payload：`docs/MOCASA催收系统升级_Phase1_领域模型与数据定义.md` §6.2、§4.1、§2.8
- 幂等 / 乱序 / 校验 / 日切：`docs/MOCASA催收系统升级_Phase1_数据接入规格.md` §3.2、§3.3、§4.3
- 金额与文案口径：`docs/channel/MOCASA催收系统升级_Phase1_渠道编排规格.md` §6.3.1；`docs/contracts/README_ContextSnapshot契约对齐.md`
- FIRM 判定：`docs/channel/MOCASA催收系统升级_Phase1_渠道编排规格.md` §6.3.1、§6.4
- 引擎实际行为：`collection-engine/.../PlanLifecycleManager.java`、`collection-ingestion/.../IngestionService.java`、`collection-common/.../enums/EventType.java`、`collection-common/.../event/CollectionEvent.java`
